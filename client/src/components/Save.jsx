import React from 'react';

export default function Save () {

  //save needs to access user id, resume, job title, company name, job text, coverletter text, 
  //db/queries/resume need addResumeByUser(userid, resume text)
  //db/queries/job_description
  //addjobDescByUser(userid, jobtitle, companyname, jobdescriptiontext)
  //addCoverLetterByJobDescription = (jobDescID, coverLetterText)

  return (
    <button>
      Save
    </button>
  )
}